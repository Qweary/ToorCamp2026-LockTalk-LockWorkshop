# Talk images

The full-resolution photos for the deck are not stored in this repo (they are
large). To drop them in, run `../rehydrate-media.sh` from the `talk/` directory
(or `talk/rehydrate-media.sh` from the repo root). See `../README.md` →
"Showing the deck with media".
