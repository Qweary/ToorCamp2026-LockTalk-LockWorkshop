# Talk video

The factory-reset demo video is not stored in this repo (it is large). To drop
it in, run `../rehydrate-media.sh` from the `talk/` directory (or
`talk/rehydrate-media.sh` from the repo root). See `../README.md` →
"Showing the deck with media".
