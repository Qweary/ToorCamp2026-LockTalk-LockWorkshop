# dumps/ — sub-manifest

Sensitive content. The single `.bin` here is a captured memory image of a real Alarm Lock T2/T3 lock board. See `../docs/DATAFLASH-DECODE-REFERENCE.md` for the dump format + validation reference.

| File | Size (bytes) | MD5 | Provenance |
|---|---:|---|---|
| `intact-lock-AT45DB041E-main-2026-05-20.bin` | 540672 | `eb6acff32ef13b29ac6ebed10d77316d` | canonical intact-lock baseline; two independent bench reads byte-identical to this MD5; used as recover-baseline.py default; AT45DB041E main array (2048 pages * 264 bytes) |
